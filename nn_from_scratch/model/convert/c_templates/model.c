#include <cstdint>
#include "model.h"

{layer_weights}
{layer_biases}

int layers_size[N_LAYERS] = {{layers_size}};
float* layers_weights[N_LAYERS] = {{layers_weights}};
float* layers_biases[N_LAYERS] = {{layers_biases}};
enum ActivationType layers_activation[N_LAYERS] = {{layers_activation}};